{
    "command_group": "az spring application-configuration-service config",
    "description": "Commands to manage the configurations pulled by Application Configuration Service from upstream Git repositories.",
    "status": "GA",
    "commands": [
        {
            "name": "az spring application-configuration-service config show",
            "summary": "Command to show the configurations pulled by Application Configuration Service from upstream Git repositories.",
            "syntax": "az spring application-configuration-service config show --config-file-pattern\n                                                        --resource-group\n                                                        --service\n                                                        [--export-path]",
            "required_parameters": [
                "--config-file-pattern --pattern",
                "--resource-group -g",
                "--service -s"
            ],
            "optional_parameters": [
                "--export-path"
            ],
            "examples": []
        }
    ]
}