{
    "command_group": "az sql db classification recommendation",
    "description": "Manage sensitivity classification recommendations.",
    "status": "GA",
    "commands": [
        {
            "name": "az sql db classification recommendation disable",
            "summary": "Disable sensitivity recommendations for a given column (recommendations are enabled by default on all columns).",
            "syntax": "az sql db classification recommendation disable --column\n                                                --schema\n                                                --table\n                                                [--ids]\n                                                [--name]\n                                                [--resource-group]\n                                                [--server]\n                                                [--subscription]",
            "required_parameters": [
                "--column",
                "--schema",
                "--table"
            ],
            "optional_parameters": [
                "--ids",
                "--name -n",
                "--resource-group -g",
                "--server -s",
                "--subscription"
            ],
            "examples": [
                "az sql db classification recommendation disable -g mygroup -s myserver -n mydb --schema dbo --table mytable --column mycolumn"
            ]
        },
        {
            "name": "az sql db classification recommendation enable",
            "summary": "Enable sensitivity recommendations for a given column (recommendations are enabled by default on all columns).",
            "syntax": "az sql db classification recommendation enable --column\n                                               --schema\n                                               --table\n                                               [--ids]\n                                               [--name]\n                                               [--resource-group]\n                                               [--server]\n                                               [--subscription]",
            "required_parameters": [
                "--column",
                "--schema",
                "--table"
            ],
            "optional_parameters": [
                "--ids",
                "--name -n",
                "--resource-group -g",
                "--server -s",
                "--subscription"
            ],
            "examples": [
                "az sql db classification recommendation enable -g mygroup -s myserver -n mydb --schema dbo --table mytable --column mycolumn"
            ]
        },
        {
            "name": "az sql db classification recommendation list",
            "summary": "List the recommended sensitivity classifications of a given database.",
            "syntax": "az sql db classification recommendation list [--filter]\n                                             [--ids]\n                                             [--include-disabled-recommendations]\n                                             [--name]\n                                             [--resource-group]\n                                             [--server]\n                                             [--subscription]",
            "required_parameters": [],
            "optional_parameters": [
                "--filter",
                "--ids",
                "--include-disabled-recommendations",
                "--name -n",
                "--resource-group -g",
                "--server -s",
                "--subscription"
            ],
            "examples": [
                "az sql db classification recommendation list -g mygroup -s myserver -n mydb"
            ]
        }
    ]
}