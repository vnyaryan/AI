{
    "command_group": "az vm encryption",
    "description": "Manage encryption of VM disks.",
    "status": "GA",
    "commands": [
        {
            "name": "az vm encryption disable",
            "summary": "Disable disk encryption on the OS disk and/or data disks. Decrypt mounted disks.",
            "syntax": "az vm encryption disable [--force]\n                         [--ids]\n                         [--name]\n                         [--resource-group]\n                         [--subscription]\n                         [--volume-type {ALL, DATA, OS}]",
            "required_parameters": [],
            "optional_parameters": [
                "--force",
                "--ids",
                "--name -n",
                "--resource-group -g",
                "--subscription",
                "--volume-type"
            ],
            "examples": [
                "az vm encryption disable --name MyVirtualMachine --resource-group MyResourceGroup --volume-type DATA"
            ]
        },
        {
            "name": "az vm encryption enable",
            "summary": "Enable disk encryption on the OS disk and/or data disks. Encrypt mounted disks.",
            "syntax": "az vm encryption enable --disk-encryption-keyvault\n                        [--aad-client-cert-thumbprint]\n                        [--aad-client-id]\n                        [--aad-client-secret]\n                        [--encrypt-format-all]\n                        [--encryption-identity]\n                        [--force]\n                        [--ids]\n                        [--key-encryption-algorithm]\n                        [--key-encryption-key]\n                        [--key-encryption-keyvault]\n                        [--name]\n                        [--resource-group]\n                        [--subscription]\n                        [--volume-type {ALL, DATA, OS}]",
            "required_parameters": [
                "--disk-encryption-keyvault"
            ],
            "optional_parameters": [
                "--aad-client-cert-thumbprint",
                "--aad-client-id",
                "--aad-client-secret",
                "--encrypt-format-all",
                "--encryption-identity",
                "--force",
                "--ids",
                "--key-encryption-algorithm",
                "--key-encryption-key",
                "--key-encryption-keyvault",
                "--name -n",
                "--resource-group -g",
                "--subscription",
                "--volume-type"
            ],
            "examples": [
                "az vm encryption enable -g MyResourceGroup -n MyVm --disk-encryption-keyvault MyVault",
                "az vm encryption enable --disk-encryption-keyvault MyVault --name MyVm --resource-group MyResourceGroup --volume-type DATA",
                "az vm encryption enable --disk-encryption-keyvault MyVault --name MyVm --resource-group MyResourceGroup --encryption-identity EncryptionIdentity"
            ]
        },
        {
            "name": "az vm encryption show",
            "summary": "Show encryption status.",
            "syntax": "az vm encryption show [--ids]\n                      [--name]\n                      [--resource-group]\n                      [--subscription]",
            "required_parameters": [],
            "optional_parameters": [
                "--ids",
                "--name -n",
                "--resource-group -g",
                "--subscription"
            ],
            "examples": [
                "az vm encryption show --name MyVirtualMachine --resource-group MyResourceGroup"
            ]
        }
    ]
}