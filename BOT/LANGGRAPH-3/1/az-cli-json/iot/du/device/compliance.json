{
    "command_group": "az iot du device compliance",
    "description": "Instance-view device compliance utilities.",
    "status": "GA",
    "commands": [
        {
            "name": "az iot du device compliance show",
            "summary": "Gets the breakdown of how many devices are on their latest update, have new updates available, or are in progress receiving new updates.",
            "syntax": "az iot du device compliance show --account\n                                 --instance\n                                 [--resource-group]",
            "required_parameters": [
                "--account -n",
                "--instance -i"
            ],
            "optional_parameters": [
                "--resource-group -g"
            ],
            "examples": [
                "az iot du device compliance show -n {account_name} -i {instance_name}"
            ]
        }
    ]
}