{
    "command_group": "az billing transaction",
    "description": "Billing transaction.",
    "status": "Preview",
    "commands": [
        {
            "name": "az billing transaction list",
            "summary": "List the transactions for an invoice. Transactions include purchases, refunds and Azure usage charges.",
            "syntax": "az billing transaction list --account-name\n                            --invoice-name",
            "required_parameters": [
                "--account-name",
                "--invoice-name"
            ],
            "optional_parameters": [],
            "examples": [
                "az billing transaction list --account-name \"{billingAccountName}\" --invoice-name \"{invoiceName}\""
            ]
        }
    ]
}