{
    "command_group": "az cosmosdb keys",
    "description": "Manage Azure Cosmos DB keys.",
    "status": "GA",
    "commands": [
        {
            "name": "az cosmosdb keys list",
            "summary": "List the access keys or connection strings for a Azure Cosmos DB database account.",
            "syntax": "az cosmosdb keys list --name\n                      --resource-group\n                      [--type {connection-strings, keys, read-only-keys}]",
            "required_parameters": [
                "--name -n",
                "--resource-group -g"
            ],
            "optional_parameters": [
                "--type"
            ],
            "examples": [
                "az cosmosdb keys list --name MyCosmosDBDatabaseAccount --resource-group MyResourceGroup --subscription MySubscription --type keys"
            ]
        },
        {
            "name": "az cosmosdb keys regenerate",
            "summary": "Regenerate an access key for a Azure Cosmos DB database account.",
            "syntax": "az cosmosdb keys regenerate --key-kind {primary, primaryReadonly, secondary, secondaryReadonly}\n                            [--ids]\n                            [--name]\n                            [--resource-group]\n                            [--subscription]",
            "required_parameters": [
                "--key-kind"
            ],
            "optional_parameters": [
                "--ids",
                "--name -n",
                "--resource-group -g",
                "--subscription"
            ],
            "examples": [
                "az cosmosdb keys regenerate --key-kind primaryReadonly --name MyCosmosDBDatabaseAccount --resource-group MyResourceGroup --subscription MySubscription"
            ]
        }
    ]
}