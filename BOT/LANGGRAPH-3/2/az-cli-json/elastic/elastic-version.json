{
    "command_group": "az elastic elastic-version",
    "description": "Manage the version of Elastic.",
    "status": "GA",
    "commands": [
        {
            "name": "az elastic elastic-version list",
            "summary": "List a list of available versions for a region.",
            "syntax": "az elastic elastic-version list --region\n                                [--max-items]\n                                [--next-token]",
            "required_parameters": [
                "--region"
            ],
            "optional_parameters": [
                "--max-items",
                "--next-token"
            ],
            "examples": [
                "az elastic elastic-version list --region myregion"
            ]
        }
    ]
}