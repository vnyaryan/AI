{
    "command_group": "az acr helm repo",
    "description": "Manage helm chart repositories for Azure Container Registries.",
    "status": "Deprecated",
    "commands": [
        {
            "name": "az acr helm repo add",
            "summary": "Add a helm chart repository from an Azure Container Registry through the Helm CLI.",
            "syntax": "az acr helm repo add --name\n                     [--password]\n                     [--suffix]\n                     [--username]",
            "required_parameters": [
                "--name -n"
            ],
            "optional_parameters": [
                "--password -p",
                "--suffix",
                "--username -u"
            ],
            "examples": [
                "az acr helm repo add -n myregistry"
            ]
        }
    ]
}