{
    "command_group": "az arcdata dc endpoint",
    "description": "Endpoint commands.",
    "status": "GA",
    "commands": [
        {
            "name": "az arcdata dc endpoint list",
            "summary": "List the data controller endpoint.",
            "syntax": "az arcdata dc endpoint list --k8s-namespace\n                            [--endpoint-name]\n                            [--use-k8s]",
            "required_parameters": [
                "--k8s-namespace -k"
            ],
            "optional_parameters": [
                "--endpoint-name -e",
                "--use-k8s"
            ],
            "examples": [
                "az arcdata dc endpoint list --k8s-namespace namespace"
            ]
        }
    ]
}