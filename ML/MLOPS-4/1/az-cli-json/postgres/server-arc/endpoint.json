{
    "command_group": "az postgres server-arc endpoint",
    "description": "Manage Azure Arc enabled PostgreSQL server endpoints.",
    "status": "Preview",
    "commands": [
        {
            "name": "az postgres server-arc endpoint list",
            "summary": "List Azure Arc enabled PostgreSQL server endpoints.",
            "syntax": "az postgres server-arc endpoint list --name\n                                     [--k8s-namespace]\n                                     [--use-k8s]",
            "required_parameters": [
                "--name -n"
            ],
            "optional_parameters": [
                "--k8s-namespace -k",
                "--use-k8s"
            ],
            "examples": [
                "az postgres server-arc endpoint list --name postgres01  --k8s-namespace namespace --use-k8s"
            ]
        }
    ]
}