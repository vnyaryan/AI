{
    "command_group": "az aks check-network",
    "description": "Commands to troubleshoot network connectivity in managed Kubernetes cluster.",
    "status": "GA",
    "commands": [
        {
            "name": "az aks check-network outbound",
            "summary": "Perform outbound network connectivity check for a node in a managed Kubernetes cluster.",
            "syntax": "az aks check-network outbound --name\n                              --resource-group\n                              [--custom-endpoints]\n                              [--node-name]",
            "required_parameters": [
                "--name -n",
                "--resource-group -g"
            ],
            "optional_parameters": [
                "--custom-endpoints",
                "--node-name"
            ],
            "examples": []
        }
    ]
}